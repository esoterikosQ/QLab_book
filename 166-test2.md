# 2차 과제

## 1. 다음 함수의 도함수를 구하시오.

### 1.1.

```math
f(x) = \int_0^x \sqrt { 1 + 4t} dt
```

### 1.2.

```math
f(x) = \int_{\log_5 x}^{x^2} 5^t dt
```

---

## 2. 미적분의 기본정리를 이용하여 다음을 해석하시오.

### 2.1. $w'(t)$는 주단위로 측정하는 아이의 몸무게 성장률이다. $\int_0^2 w'(t) dt$는 무엇을 의미하는가?

### 2.2. $I'(t)$는 연간 소득의 변화율을 보여주는 함수이다. $\int_0^{10} I'(t) dt$는 무엇을 의미하는가?

---

## 3. 다음 두 곡선으로 둘러싸인 영역의 면적을 구하시오.

### 3.1. $y = x^2$, $y = x^4$

### 3.2. $x = y^2 - y$, $x = 3y - y^2$

### 3.3. $x + y = 0$, $x = y^2 + 4y$

---

## 4. 다음 부피를 구하시오.

### 4.1. $y = x^{2/3}$, $y = 1$, $x = 0$으로 둘러싸인 영역을 $y$축을 중심으로 회전시켜 생기는 입체의 부피

### 4.2. $y = 2x - x^2$, $y = x$로 둘러싸인 영역을  $y = x$에 대해 회전했을 때에 생기는 입체의 부피

---

## 5. 적분의 평균값 정리를 이용하여 $f(x) = x^3$함수의 $[-1, 2]$ 구간에서의 평균을 계산하고, 평균값과 함수값이 일치하는 지점의 x를 계산하시오.

--- 

## 6. 다음 적분을 계산하시오.

### 6.1. 

```math
\int (x+4)^{100} dx
```

### 6.2.

```math
\int \frac{3+x}{1+x^2} dx
```

### 6.3.

```math
\int_0^3 x \sqrt {x+1} dx
```

### 6.4.

```math
\int x e^x dx
```

### 6.5.

```math
\int x \sin {5x} dx
```

### 6.6.

```math
\int_1^9 e^{\sqrt x} dx
```