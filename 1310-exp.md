# 기댓값

## 정의

```{prf:definition} 기댓값 expectation

확률변수 $X$가 확률밀도함수 $f(x)$를 가질 때, $X$의 함수인 $u(X)$의 기댓값은 $\mathbb E [ u(X)]$로 표기하고 다음과 같이 정의한다.

$$ \mathbb E[u(X)] = \begin{cases} \sum_x u(x)f(x), \ \text{이산형} \\ \int_{-\infty}^{\infty} u(x)f(x) dx, \ \text{연속형} \end{cases} $$

```

## 기댓값의 형태

### 평균

$u(X) = X$일 때의 기댓값을 **기댓값** 또는 **평균**(mean)이라고 한다. 분포함수의 무게 중심을 나타낸다.

### 분산

$u(X) = (X - \mu)^2$일 때의 기댓값을 **분산**(variance)이라고 한다. 분산의 제곱근을 **표준편차**(standard deviation, s.d.)라고 한다. 표준편차는 원데이터와 단위를 맞추기 위해 사용한다. 

### 적률생성함수

$u(X) = e^{tX}$일 때의 기댓값을 **적률생성함수**(moment generating function, m.g.f.)라고 한다. 적률생성함수는 cdf와 마찬가지로 분포의 고유한 속성과 연결되기 때문에 적률생성함수가 같은 두 확률변수는 동일한 분포를 따른다.

**적률**(moment)은 분포의 **중심**에 대응하는 용어라고 이해할 수 있다. 도형에서 중심에 해당하는 다양한 개념이 존재하듯, 분포의 중심에 해당하는 다양한 개념이 존재한다. 1차 적률은 평균이다. 다른 수리적 가공이 없는 분포함수에서 각 값이 가지는 확률값을 가중했을 때의 중심값이라고 이해할 수 있다. 2차 중심적률은 분산, 3, 4차 표준중심적률은 각각 왜도, 첨도에 해당한다. 이들 값은 각각 평균으로부터 분포가 흩어진 정도, 기울어진 정도, 평균으로 몰린 정도를 보여주는 지표이다. 적률생성함수는 각 차수별 도함수값을 이용하여 이들 통계량을 계산할 수 있도록 도와주는 역할을 한다.

mgf를 $t=0$에서 테일러 전개하면 다음과 같다.

```{math}
\begin{aligned}
M(t) &= M(0) + \frac {M'(0)}{1!}t + \frac {M''(0)}{2!}t^2 + \cdots + \frac {M^{(m)}(0)}{m!}t^m + \cdots \\

&= 1 + \frac {\mathbb E{X}}{1!}t + \frac {\mathbb E{X^2}}{2!}t^2 + \cdots + \frac {\mathbb E{X^m}}{m!}t^m + \cdots 

\end{aligned}
```

따라서, $\frac{t^m}{m!}$의 계수가 $\mathbb E(X^m)$이 되고, 역으로 모든 차수의 적률을 알고 있다면 mgf를 구할 수 있다.


## CDF와 기댓값

CDF를 이용하여 기댓값과 분산을 다음과 같이 구할 수 있다.


```{prf:definition} 기댓값과 분산

확률변수 $X$에 대해 다음이 성립한다.

$$ \mathbb E[X] = \int_0^{\infty} [1 - F(x)] dx - \int_{-\infty}^0 F(x) dx $$

$$ Var(X) = \int_0^{\infty} 2x [1 - F(x)] + F(-x) ] dx - \mathbb E (X)^2 $$

```